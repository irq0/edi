#!/bin/sh

exec 2>&1

cd ${APPDIR}/tts-pico2wave

exec chpst -e "$ENVDIR" \
     listen_command \
     --cmd say \
     --cmd tts \
     --data onlyargs \
     --name "tts" \
     --description "pico2wave based tts" \
     --exe "${APPDIR}/tts-pico2wave/edimq-handler"
